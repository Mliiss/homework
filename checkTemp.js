function checkTemp(temperatuur) {
    if (temperatuur >= 0 && temperatuur <= 20 ) {
        console.log ("-1");
    }  else if (temperatuur <= 40 ) {
        console.log ("0");
    }  else (temperatuur <= 60 ) 
        console.log ("1");
    }
